import React from 'react';

/** En web no hay SDK de AdMob disponible; no renderiza nada por ahora
 * (ver README, sección "Distribución sin tiendas"). */
export default function BannerAd() {
  return null;
}
